import { Component } from "preact";

interface VereinProps {}
interface VereinState {}
export default class Verein extends Component<VereinProps, VereinState> {
    render = () => {
        return <div className="Verein"></div>;
    };
}
