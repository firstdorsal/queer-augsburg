import { Component } from "preact";

interface TreffenProps {}
interface TreffenState {}
export default class Treffen extends Component<TreffenProps, TreffenState> {
    render = () => {
        return <div className="Treffen"></div>;
    };
}
