import { Component } from "preact";

interface KontaktProps {}
interface KontaktState {}
export default class Kontakt extends Component<KontaktProps, KontaktState> {
    render = () => {
        return <div className="Kontakt"></div>;
    };
}
